[project]
name = "extrasuite-docs"
version = "0.1.0"
description = "ExtraSuite documentation"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.5.0",
    "mkdocs-minify-plugin>=0.7.0",
]

[tool.uv]
dev-dependencies = []

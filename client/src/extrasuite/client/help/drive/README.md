Browse and search Google Drive files visible to the service account.

## Subcommands

  extrasuite drive ls       List files (ordered by last modified)
  extrasuite drive search   Search files using a Drive query string

## Notes

- Results are limited to files explicitly shared with your service account.
- Use `extrasuite auth login` first if you have not authenticated yet.

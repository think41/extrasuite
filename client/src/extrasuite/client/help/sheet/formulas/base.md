# BASE

BASE(value, base, [min_length])

Converts a number into a text representation in another base, for example, base 2 for binary. .

To learn more, see https://support.google.com/docs/answer/9084167

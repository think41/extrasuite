# SUBSTITUTE

SUBSTITUTE(text_to_search, search_for, replace_with, [occurrence_number])

Replaces existing text with new text in a string.

To learn more, see https://support.google.com/docs/answer/3094215

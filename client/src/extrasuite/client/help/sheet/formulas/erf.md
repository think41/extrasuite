# ERF

ERF(lower_bound, [upper_bound])

The ERF function returns the integral of the Gauss error function over an interval of values. .

To learn more, see https://support.google.com/docs/answer/9116267

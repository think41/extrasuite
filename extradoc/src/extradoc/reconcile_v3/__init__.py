"""reconcile_v3: top-down tree diff experiment for Google Docs.

This is a self-contained in-memory experiment.  It does NOT integrate with
the production pipeline — use reconcile_v2 for production use.
"""
